/* @pre none
 * @post This method generates a random x value
 * @param none
 * @return returns the random generated x value
 * */
function getRandomX()

/* @pre none
 * @post This method generates a random y value
 * @param none
 * @return returns the random generated y value
 * */
function getRandomY()

/* @pre none
 * @post This method creates an array for the AI's ships at level hard
 * @param none
 * @return none
 * */
function generatearrayforAIhard()

/* @pre none    
 * @post This method is used to get AI corrd for filling the display board
 * @param none
 * @return none
 * */
function returnXCoordforAIhard()

/* @pre none    
 * @post This method is used to get AI corrd for filling the display board
 * @param none
 * @return none
 * */
function returnYCoordforAIhard()

/* @pre none
 * @post This method keeps track of the places the AI can and can't shot a ship for level medium
 * @param
 * @return
 * */
function generatearrayforAImedium()

/* @pre none
 * @post This method once a ship is hit tells the AI where to go next to finish the ship
 * @param row and col
 * @return none
 * */
function recursiveMedium(row, col)

/* @pre
 * @post This method randomly picks places for the AI to hit ships at medium then once has a hit calls recursiveMedium
 * @param return
 * @return return 
 * */
function generateAImoves()

/* @pre none
 * @post This method takes the given x and y and changes that box grey
 * @param x and y
 * @return the spot changes to grey
 * */
function graySpace(x, y)

/* @pre none
 * @post This method randomly places AI ships when the player chooses 1 ship to play with
 * @param none
 * @return placed ship in AI's array of ships
 * */
function generateRandom1shipCoordinate()

/* @pre none
 * @post This method randomly places AI ships when the player chooses 2 ship to play with
 * @param none
 * @return placed ship in AI's array of ships
 * */
function generateRandom2shipCoordinate()

/* @pre none
 * @post This method randomly places AI ships when the player chooses 3 ship to play with
 * @param none
 * @return placed ship in AI's array of ships
 * */
function generateRandom3shipCoordinate()

/* @pre none
 * @post This method randomly places AI ships when the player chooses 4 ship to play with
 * @param none
 * @return placed ship in AI's array of ships
 * */
function generateRandom4shipCoordinate()

/* @pre none
 * @post This method randomly places AI ships when the player chooses 5 ship to play with
 * @param none
 * @return placed ship in AI's array of ships
 * */
function generateRandom5shipCoordinate()

/* @pre none
 * @post This method randomly places AI ships when the player chooses 6 ship to play with
 * @param none
 * @return placed ship in AI's array of ships
 * */
function generateRandom6shipCoordinate()
